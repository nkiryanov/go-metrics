---
services:
  postgres:
    image: postgres:17-alpine
    ports: 
      - "15432:5432"
    environment:
      POSTGRES_DB: go-metrics
      POSTGRES_USER: go-metrics
      POSTGRES_HOST_AUTH_METHOD: trust
